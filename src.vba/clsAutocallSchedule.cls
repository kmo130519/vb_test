Option ExplicitnnnPublic call_date As DatennnPrivate percent_strike_() As DoublenPrivate coupon_on_call_() As DoublenPrivate strike_value_() As Doublen'<---addednPublic performance_type As LongnPublic strike_shift As DoublenPublic ejectable_order As Long 'dll(2021.11.12)nPublic ejected_event_flag As Long 'dll(2021.11.12)n'--->nnnPublic Function no_of_jumps() As Integern    n    Dim rtn_value As Integern    n    n    rtn_value = get_array_size_double(percent_strike_) '<------------------------------n    n    no_of_jumps = rtn_valuennEnd FunctionnnPublic Function copy_obj() As clsAutocallSchedulenn    Dim rtn_obj As clsAutocallSchedulen    Dim inx As Integern    nOn Error GoTo ErrorHandlernn    Set rtn_obj = New clsAutocallSchedulen    n    rtn_obj.call_date = call_daten    rtn_obj.performance_type = performance_typen    rtn_obj.strike_shift = strike_shiftn    rtn_obj.ejectable_order = ejectable_ordern    rtn_obj.ejected_event_flag = ejected_event_flagn    n    For inx = 1 To get_array_size_double(percent_strike_)n        rtn_obj.set_percent_strike percent_strike_(inx), inxn    Next inxn    n    For inx = 1 To get_array_size_double(coupon_on_call_)n        rtn_obj.set_coupon_on_call coupon_on_call_(inx), inxn    Next inxn    n    For inx = 1 To get_array_size_double(strike_value_)n        rtn_obj.set_strike_value strike_value_(inx), inxn    Next inxn    n    Set copy_obj = rtn_objn    n    Exit Functionn    nErrorHandler:nn    raise_err "clsAutocallSchedule.copy_obj", Err.descriptionnnEnd FunctionnnPublic Function percent_strike(Optional jump_no As Integer = 1) As Doublenn    percent_strike = percent_strike_(jump_no)nnEnd FunctionnnPublic Function coupon_on_call(Optional jump_no As Integer = 1) As Doublenn    coupon_on_call = coupon_on_call_(jump_no)nnEnd FunctionnnPublic Function strike_value(Optional jump_no As Integer = 1) As Doublenn    strike_value = strike_value_(jump_no)nnEnd FunctionnnPublic Sub set_coupon_on_call(cpn_value As Double, Optional jump_no As Integer = 1)nn    If UBound(coupon_on_call_) < jump_no Thenn        push_back_double coupon_on_call_, cpn_valuen    Elsen        coupon_on_call_(jump_no) = cpn_valuen    End IfnnEnd SubnnPublic Sub set_percent_strike(pct_strike As Double, Optional jump_no As Integer = 1)nn    If UBound(percent_strike_) < jump_no Thenn        push_back_double percent_strike_, pct_striken    Elsen        percent_strike_(jump_no) = pct_striken    End IfnnEnd SubnnPublic Sub set_strike_value(strike As Double, Optional jump_no As Integer = 1)nn    If UBound(strike_value_) < jump_no Thenn        push_back_double strike_value_, striken    Elsen        strike_value_(jump_no) = striken    End IfnnEnd SubnnPrivate Sub Class_Initialize()nn    ReDim percent_strike_(1 To 1) As Doublen    ReDim coupon_on_call_(1 To 1) As Doublen    ReDim strike_value_(1 To 1) As DoublennEnd Sub