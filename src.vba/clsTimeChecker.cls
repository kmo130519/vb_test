Option Explicitnn'Private Declare Function GetTickCount Lib "kernel32.dll" () As LongnnnPrivate tick_counter_() As LongnPrivate stop_point_name_() As StringnnPublic Sub initailize()n    n    Erase tick_counter_n    Erase stop_point_name_nnEnd SubnnnnnPublic Function get_array_count() As Longn    n    Dim rtn_value As Longn    nOn Error Resume Nextnn    rtn_value = UBound(tick_counter_)n    n    If Err.number = 9 Thenn    n        rtn_value = 0n        n    End Ifn    n    n    get_array_count = rtn_valuennnEnd FunctionnnPublic Sub add_tick_counter(ByVal stop_point_name As String)nn    Dim inx As Integern    Dim current_size As Integern    nOn Error Resume Nextn    current_size = UBound(tick_counter_)n    n    If Err.number = 9 Thenn        n        current_size = 0n    n    End Ifn    nOn Error GoTo ErrorHandlernn    ReDim Preserve tick_counter_(1 To current_size + 1) As Longn    ReDim Preserve stop_point_name_(1 To current_size + 1) As Stringn    n    tick_counter_(UBound(tick_counter_)) = GetTickCountn    stop_point_name_(UBound(stop_point_name_)) = stop_point_namenn    Exit Subn    nErrorHandler:nn    raise_err "clsTimeChecker.add_tick_counter"n    nnEnd SubnnnnPublic Function tick_counter() As Long()nn    tick_counter = tick_counter_nnEnd FunctionnnPublic Function stop_point_name() As String()nn    stop_point_name = stop_point_name_nnEnd Functionn